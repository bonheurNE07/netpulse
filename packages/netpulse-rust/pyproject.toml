[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "netpulse-rust"
version = "0.1.0"
description = "Native Rust bindings for netpulse tools."
requires-python = ">=3.10"

[tool.maturin]
module-name = "netpulse_rust"
manifest-path = "../../rust/Cargo.toml"
